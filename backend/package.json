{
  "name": "omnisect-backend",
  "version": "1.1.0",
  "description": "Omnisect — automated bug bounty reconnaissance with SSRF guard, auth, scope file, SSE streaming, persistence.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "vitest run",
    "evaluate:planner": "node scripts/evaluatePlanner.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^8.3.0",
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "vitest": "^1.6.0"
  }
}
