# Copy to backend/.env or set in your shell before launching.

PORT=3001
HOST=127.0.0.1
# HOST=0.0.0.0   # only if you need mobile LAN clients

# OMNISECT_ROOT=
# OMNISECT_TOOLS_DIR=
# OMNISECT_DATA_DIR=
# SURFACE_MAPPER_BIN=

# DANGEROUS — non-default, local-dev only. Backend logs a loud warning if set.
# ALLOWED_ORIGINS=*
# DISABLE_AUTH=true
